.venv/
__pycache__/
*.pyc
//data/
*.egg-info/
.pytest_cache/
# Trained model artifacts are regenerable outputs of `futmarket train`,
# not source. The model_runs table records which artifact produced which metrics.
data/models/

# Credentials — never commit these
.env
.x_session.json
