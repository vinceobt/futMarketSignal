# Python
.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/

# Node — never track dependencies (the old frontend is gone)
node_modules/
frontend/

# Data warehouse, logs, generated scripts, trained models — regenerable + large
data/

# Credentials — never commit
.env
.x_session.json
